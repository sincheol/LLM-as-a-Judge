# LLM-as-a-Judge 위치 편향 원인 규명 및 극복 실험 결과

본 문서는 Gemini-3-Flash-Preview 모델을 심판으로 활용할 때 발생하는 위치 편향(Positional Bias) 원인을 분석한 보고서입니다. 프롬프트 엔지니어링의 한계를 확인하고, 디코딩 제어(Constrained Decoding)와 로짓 정규화를 통해 이를 완화한 실험 결과를 정리했습니다.

---

> **참고: 위치 편향(Positional Bias)이란?**
> 위치 편향은 모델이 답변의 품질을 평가하지 않고 입력된 순서에 영향을 받아 승자를 결정하는 현상입니다. 예를 들어 화면에 마지막으로 제시된 답변을 무조건 승자로 선택하는 맹점을 의미합니다.

---

## PART 1. 프롬프트 제어를 통한 편향 완화 실험 (Track 1)

LLM의 주의력 매커니즘이 지니는 최신 편향(Recency Bias)을 프롬프트 개선으로 해결할 수 있는지 검증했습니다.

| 실험 전략 | 메커니즘 요약 | N | [A-B] 배열 시 A 승리 | [B-A] 배열 시 A 승리 | 전체 위치 편향률 (Second Position Wins) | 결과 불일치(TIE) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **BPC** (Balanced Position Calibration) | 프롬프트 입력 순서를 [A-B], [B-A]로 2회 호출하여 교차 검증 | 100 | 8% (4/50) | 10% (5/50) | **55.6%** (유효 판정 기준) | 91.0% |
| **MEC** (Multiple Evidence Correction) | 승자 판정 전, 양쪽 답변에 대한 분석을 강제 | 100 | 20% (10/50) | 88% (44/50) | **84.0% (편향 악화)** | - |
| **CAI** (Constitutional AI) | System Prompt에 "균형적 평가" 등 원칙 주입 | 100 | 4% (2/50) | 94% (47/50) | **95.0% (최악의 편향)** | - |

### Track 1 분석 결과
1. **명시적 지시의 부작용**: LLM에게 편향을 피하라거나 양측을 모두 분석하라고 지시할수록 오히려 마지막 토큰에 주의력이 과도하게 쏠렸습니다. 그 결과 편향이 최대 95%까지 악화되었습니다.
2. **단방향 추론의 한계**: 단일 프롬프트 환경에서는 단순한 프롬프팅 기법만으로 구조적 편향을 교정하기 어렵습니다.
3. **교차 검증의 한계**: 순서를 바꿔 2번 검증(BPC)하면 편향된 응답을 걸러낼 수 있습니다. 하지만 두 번의 평가 결과가 서로 엇갈리는 **결과 불일치(TIE)** 현상이 91%나 발생하여, 실제 서비스에서는 승자를 영원히 낼 수 없는 치명적인 단점이 있습니다.

---

## PART 2. Constrained Decoding 및 로짓 교정 기법 (Track 2)

단순 텍스트 제어의 한계를 극복하기 위해, 모델의 토큰 생성 확률(Logprobs) 수준에서 편향을 제어했습니다.

### 2.1. 디코딩 메커니즘 수정 방식
*   **문제점**: Flash 모델은 내부 추론 능력이 강합니다. 출력 길이를 1자로 제한(`max_output_tokens=1`)하면 추론 토큰만 출력되어 결과 확률 공간을 추적하기 어려웠습니다.
*   **해결책**: Vertex AI 인프라에서 `response_mime_type="text/x.enum"` 과 `{"enum": ["1", "2"]}` 스키마를 주입했습니다. 이를 통해 모델의 출력 확률 풀을 ["1", "2"] 두 개로 제한했습니다. 구조적으로 추론 단계를 건너뛰어 예측 로짓을 온전히 캡처했습니다.

### 2.2. PAIRS 논문의 불확실성 추출 및 로짓 교정 기법
추출된 "1"과 "2" 토큰의 확률값에 Softmax를 적용하여 정규화했습니다. 이후 프롬프트 순서를 스왑한 양방향 평균을 산출하여 최종 확신도(Confidence Score)를 도출했습니다.

### 종합 실험 결과 (gemini-3-flash-preview 기준, 100회)
| 지표 | 비보정 텍스트 | 불확실성 추출 기법 도입 결과 |
| :--- | :--- | :--- |
| **[A-B] 배열 시 A 승리** | 100% | 100% |
| **[B-A] 배열 시 A 승리** | 100% | 100% |
| **전체 위치 편향률** | **0%** | - |
| **평균 교정 확률** | - | **$P(\text{A}) \approx 99.8\%$**, $P(\text{B}) \approx 0.2\%$ |

### Track 2 분석 결과
1. **생성 제약의 효과**: 생성 제약(Constrained Decoding)을 적용하자 위치 편향이 0%로 줄었습니다. 모델에게 출력 자유도를 주지 않고 직관적인 마킹만 요구했을 때 객관적인 평가 능력이 회복되었습니다.
2. **모델의 확신도와 엔트로피 해석**: 정규화된 확률값은 엔트로피 기반 불확실성 공식으로 설명할 수 있습니다. 확률이 한쪽으로 뚜렷하게 쏠릴 경우 엔트로피는 0에 수렴합니다. 이는 프롬프트 순서 변경 같은 노이즈에 모델이 흔들리지 않고 확실한 판정을 내렸음을 의미합니다.
3. **무승부 방지**: 별도의 무승부(TIE) 처리 없이 명확한 승패 판정을 도출했습니다.

---

## 종합 결론

자연어 프롬프트 트릭(CAI, MEC, BPC)에 의존하여 위치 편향을 해결하는 방식은 한계가 명확합니다(최대 95% 편향 재발). 신뢰할 수 있는 판정 시스템을 구축하기 위해서는 구조적인 접근이 필요합니다.

### 추천 아키텍처
**[Constrained Decoding + PAIRS 논문의 로짓 교정 기법]**
1. **API 호출**: Vertex AI 환경에서 Enum 타입 응답을 지정하여 평가를 요청합니다.
2. **평가 방식**: 양방향(A-B, B-A) 쌍별 프롬프트를 보내 확률(Logprobs)을 추출합니다.
3. **백엔드 처리**: 추출된 확률을 Softmax로 변환하고 양방향 평균을 내어 최종 결과를 산출합니다.
4. **기대 효과**: 위치 편향과 무승부 문제를 동시에 해결하고 채점 신뢰도를 확보할 수 있습니다.

### 교정 로직을 유지해야 하는 이유
Constrained Decoding만으로 편향이 해결되더라도 논문에 제시된 확률 교정 로직을 유지해야 하는 이유는 다음과 같습니다.

*   **초박빙 상황 대비**: 두 답변의 품질 차이가 적을 때 Enum 제약 상황에도 무승부 결론이 날 수 있습니다. 이 때 소수점 평균 계산을 통해 최종 판정이 가능합니다.
*   **안전망 확보**: 향후 모델 업데이트로 편향 문제가 재발하더라도 양방향 평균 산출 로직이 이를 방어합니다.
*   **사용자 경험 개선**: 단순 승패가 아닌 확률 기반 확신도(Confidence Score)를 제공하여 판정의 설득력을 높일 수 있습니다.

참고 자료:
*   [Google Developers 개요](https://developers.googleblog.com/unlock-gemini-reasoning-with-logprobs-on-vertex-ai/)
*   [GCP Github 예제](https://github.com/GoogleCloudPlatform/generative-ai/blob/main/gemini/logprobs/intro_logprobs.ipynb)